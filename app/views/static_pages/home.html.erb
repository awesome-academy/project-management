<% provide :title, t("static.home") %>

<div class="center jumbotron">
  <h1><%= t "static.welcome" %></h1>
  <% if logged_in? %>
    <%= link_to t("project.all_project"), projects_path, class: "btn btn-lg btn-primary" %>
    <% else %>
    <%= link_to t("static.sign_up"), new_user_path, class: "btn btn-lg btn-primary" %>
  <% end %>
</div>
